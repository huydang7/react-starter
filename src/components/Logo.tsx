import React from 'react';
import { Typography } from 'antd';

const Logo = () => {
  return (
    <div className="mb-1">
      <Typography.Title level={1}>Logo</Typography.Title>
    </div>
  );
};

export default Logo;
